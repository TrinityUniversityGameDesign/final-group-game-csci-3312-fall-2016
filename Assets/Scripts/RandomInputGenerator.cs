using UnityEngine;
using System.Collections;

public class RandomInputGenerator : MonoBehaviour {

	public enum Buttons {A,B,Y,X};

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
